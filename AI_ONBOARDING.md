# AI Agent Onboarding Guide
# Руководство для нового AI агента по проекту Catan

**Последнее обновление:** 2025-10-30
**Статус проекта:** Phase 0-3 завершены (44%), Phase 4-8 в разработке

---

## 🎯 Цель проекта

Разработка **полнофункциональной настольной игры Catan** (Колонизаторы) на Next.js + TypeScript с возможностью игры против AI противников.

**Ключевые особенности:**
- Полная реализация правил Catan
- Гексагональная карта с offset grid layout
- AI оппоненты с эвристиками
- Система достижений и статистики
- **100% покрытие семантической разметкой** для AI-навигации

---

## 📁 Структура проекта и где что искать

### 1. 📚 Документация (приоритет чтения)

**Начни отсюда:**
```
docs/
├── TechSpecification.md          # ТЗ (~2700 строк) - ГЛАВНЫЙ документ требований
├── DevelopmentPlan.md            # XML граф функций + алгоритмы (~2700 строк)
├── ImplementationRoadmap.md      # Пошаговый план по фазам (~1000 строк)
├── CodeGenerationGuide.md        # Примеры кода с разметкой (~600 строк)
├── TechStackRationale.md         # Обоснование технологий (~1600 строк)
└── CLAUDE.md                     # Инструкции по работе с проектом
```

**Порядок чтения для быстрого старта:**
1. **CLAUDE.md** (5 мин) - как работать с семантической разметкой
2. **ImplementationRoadmap.md** (10 мин) - где мы сейчас, что дальше
3. **CodeGenerationGuide.md** (15 мин) - примеры правильного кода
4. **TechSpecification.md** (по необходимости) - детальные требования

### 2. 🗺️ Граф знаний (для RAG)

```
knowledge_graph.xml               # XML граф всего проекта для семантического поиска
```

**Что он содержит:**
- Все модули, функции, классы, компоненты
- Связи между модулями (CALLS, DEPENDS_ON, USES_DATA_FROM)
- Keywords и terms для поиска
- Статус: COMPLETED vs PLANNED
- 860+ строк структурированной информации

**Как использовать:**
- Поиск по keywords: `<keywords>longest_road:8|dfs:9</keywords>`
- Поиск функций: `<lib_game_dash_logic_calculators_calculateLongestRoad_FUNC>`
- Понимание зависимостей через `<CrossLinks>`

### 3. 💻 Кодовая база

```
types/
├── game.types.ts                 # ВСЕ типы игры (ResourceType, GameState, Player)
└── ai.types.ts                   # Типы для AI (AIAction, AIStrategy)

lib/
├── constants/game.constants.ts   # Все игровые константы и правила
├── game-logic/
│   ├── mapGenerator.ts          # Генерация карты (DFS, Fisher-Yates)
│   ├── validators.ts            # Валидация действий
│   ├── calculators.ts           # VP, longest road (DFS), распределение ресурсов
│   └── actionHandlers.ts        # Обработка действий игроков
└── utils/gameUtils.ts           # Утилиты (rollDice, createPlayer, etc.)

components/
├── board/
│   ├── Board.tsx                # Главный компонент игрового поля (SVG)
│   ├── Hex.tsx                  # Компонент гексагона
│   ├── Vertex.tsx               # Компонент вершины (settlement/city)
│   └── Edge.tsx                 # Компонент ребра (road)
└── ui/
    ├── Button.tsx               # Переиспользуемая кнопка
    └── PlayerPanel.tsx          # Панель информации об игроке

app/
├── layout.tsx                   # Root layout
├── page.tsx                     # Домашняя страница (меню)
└── globals.css                  # Глобальные стили + CSS переменные
```

---

## 🔑 Ключевые концепции проекта

### 1. Семантическая разметка кода

**КРИТИЧЕСКИ ВАЖНО:** Весь код должен следовать semantic_template.txt

**Обязательные элементы:**
```typescript
/**
 * START_MODULE_[name]
 *
 * MODULE_CONTRACT:
 * PURPOSE: [Что делает модуль]
 * SCOPE: [Область ответственности]
 * KEYWORDS: [keyword1, keyword2]
 * LINKS_TO_MODULE: [Связанные модули]
 */

// START_FUNCTION_[FunctionName]
// FUNCTION_CONTRACT:
// PURPOSE: [Что делает функция]
// INPUTS: [параметры]
// OUTPUTS: [возвращаемое значение]
// SIDE_EFFECTS: [побочные эффекты или None]
export function functionName() {
  // START_BLOCK_[BLOCK_NAME]
  // Описание: [Что делает блок]

  // код

  // END_BLOCK_[BLOCK_NAME]
}
// END_FUNCTION_[FunctionName]

/**
 * END_MODULE_[name]
 */
```

**Почему это важно:**
- Позволяет AI агентам навигироваться по коду
- Позволяет быстро находить нужные функции через Grep
- Связывает логи с блоками кода
- Документирует назначение каждого блока

### 2. Игровая логика Catan

**Основные сущности:**
- **Hex** (гексагон) - тайл карты с местностью и номером токена
- **Vertex** (вершина) - место для settlement/city
- **Edge** (ребро) - место для road
- **Player** - игрок с ресурсами, постройками, картами развития

**Граф карты:**
```
Hexes (19 штук) в offset grid (3-4-5-4-3)
    ↓
Vertices (54 штуки) - пересечения 3-х гексов
    ↓
Edges (72 штуки) - линии между вершинами
```

**Ключевые алгоритмы:**
- **DFS для longest road** - `calculators.ts:calculateLongestRoad()`
- **Distance rule для settlements** - минимум 2 ребра между поселениями
- **Resource distribution** - при броске кубиков раздача ресурсов

**Фазы игры:**
1. `INITIAL_PLACEMENT` - начальная расстановка (2 круга)
2. `MAIN_GAME` - основная игра
3. `GAME_OVER` - конец игры

**Фазы хода:**
1. `DICE_ROLL` - бросок кубиков
2. `ROBBER_ACTIVATION` - активация разбойника (при 7)
3. `ACTIONS` - действия игрока

### 3. Технологический стек

**Framework:**
- Next.js 16.0.1 (App Router)
- React 19.2.0
- TypeScript 5.9.3 (strict mode)

**State Management:**
- Zustand 5.0.8 (будет реализовано в Phase 4)

**Styling:**
- Tailwind CSS 3.4.1 (НЕ v4! - откатились из-за проблем)
- CSS переменные в globals.css

**Иконки:**
- Lucide React 0.548.0

**Важно:**
- `package.json` НЕ содержит `"type": "commonjs"` (используем ES modules)
- `tailwind.config.js` (не .ts) для совместимости

---

## 📊 Текущий прогресс

### ✅ ЗАВЕРШЕНО (Phase 0-3):

**Phase 0 - Infrastructure:**
- ✅ Next.js проект настроен
- ✅ TypeScript strict mode
- ✅ Tailwind CSS v3 настроен
- ✅ Структура папок создана

**Phase 1 - Data Models:**
- ✅ `types/game.types.ts` - все enums и interfaces
- ✅ `types/ai.types.ts` - AI типы
- ✅ `lib/constants/game.constants.ts` - все константы

**Phase 2 - Core Game Logic:**
- ✅ `lib/game-logic/mapGenerator.ts` (~300 строк)
- ✅ `lib/game-logic/validators.ts` (~350 строк)
- ✅ `lib/game-logic/calculators.ts` (~350 строк) + DFS для longest road
- ✅ `lib/game-logic/actionHandlers.ts` (~350 строк)
- ✅ `lib/utils/gameUtils.ts` (~250 строк)

**Phase 3 - UI Foundation:**
- ✅ `components/board/Board.tsx` - главный компонент поля
- ✅ `components/board/Hex.tsx` - гексагон
- ✅ `components/board/Vertex.tsx` - вершина с постройками
- ✅ `components/board/Edge.tsx` - ребро с дорогой
- ✅ `components/ui/Button.tsx` - кнопка
- ✅ `components/ui/PlayerPanel.tsx` - панель игрока
- ✅ `app/layout.tsx` + `app/page.tsx` - домашняя страница

**Статус:** TypeScript компилируется без ошибок, `npm run dev` работает ✅

### 🚧 В РАЗРАБОТКЕ (Phase 4 - следующая):

**Phase 4 - Basic Gameplay:**
- [ ] `store/gameStore.ts` - Zustand store
- [ ] `app/game/page.tsx` - главный игровой экран
- [ ] `components/game/DicePanel.tsx` - панель кубиков
- [ ] `components/game/ActionPanel.tsx` - панель действий
- [ ] Начальная расстановка (initial placement)
- [ ] Бросок кубиков и распределение ресурсов
- [ ] Базовые действия (build road/settlement/city)
- [ ] Смена хода

**Смотри:** `docs/ImplementationRoadmap.md` Phase 4 для детального плана

### 📋 ЗАПЛАНИРОВАНО (Phase 5-8):

- Phase 5: Торговля, карты развития, разбойник, badges
- Phase 6: AI оппонент
- Phase 7: Достижения и статистика
- Phase 8: Анимации, звуки, полировка

---

## 🛠️ Как начать работу

### Сценарий 1: Продолжить с Phase 4

1. **Прочитай план:**
   ```bash
   Read docs/ImplementationRoadmap.md (Phase 4 секция)
   ```

2. **Начни с Zustand store:**
   ```bash
   Read docs/CodeGenerationGuide.md (Пример Zustand store)
   ```

3. **Создай `store/gameStore.ts`:**
   - Используй semantic template
   - Имплементируй actions: initializeGame, rollDice, buildRoad, etc.
   - Интегрируй с `lib/game-logic/actionHandlers.ts`

4. **Создай игровой экран `app/game/page.tsx`:**
   - Подключи Board, PlayerPanel
   - Подключи gameStore
   - Реализуй начальную расстановку

### Сценарий 2: Исправить баг в существующем коде

1. **Найди нужный модуль:**
   ```bash
   # Через граф знаний
   Read knowledge_graph.xml
   # Ищи по keywords или названию функции

   # Или через Grep
   Grep pattern="START_FUNCTION_calculateLongestRoad"
   ```

2. **Прочитай CONTRACT функции:**
   - Поймёшь PURPOSE, INPUTS, OUTPUTS
   - Найдёшь LINKS к связанным функциям

3. **Изучи логические блоки:**
   - START_BLOCK / END_BLOCK покажут структуру
   - Логи привязаны к блокам

4. **Исправь и обнови:**
   - Сохрани семантическую разметку
   - Обнови CONTRACT если сигнатура изменилась

### Сценарий 3: Понять как работает механика X

1. **Начни с TechSpecification.md:**
   ```bash
   Grep pattern="LOGIC_BLOCK_X" path=docs/TechSpecification.md
   ```

2. **Найди реализацию:**
   ```bash
   # В графе знаний
   Read knowledge_graph.xml
   # Ищи по keywords (например "longest_road")

   # В коде
   Grep pattern="START_FUNCTION_calculateLongestRoad"
   ```

3. **Изучи алгоритм:**
   ```bash
   Read docs/DevelopmentPlan.md
   # Секция "Описание алгоритма step-by-step"
   ```

---

## ⚠️ Важные правила и ограничения

### 1. Семантическая разметка ОБЯЗАТЕЛЬНА

❌ **НЕ делай так:**
```typescript
function rollDice() {
  const die1 = Math.random() * 6 + 1;
  return [die1, die2];
}
```

✅ **Делай так:**
```typescript
// START_FUNCTION_rollDice
// FUNCTION_CONTRACT:
// PURPOSE: Бросить два кубика
// INPUTS: None
// OUTPUTS: [number, number] - значения двух кубиков
// SIDE_EFFECTS: Использует Math.random
export function rollDice(): [number, number] {
  // START_BLOCK_DICE_GENERATION
  // Описание: Генерация случайных значений кубиков

  const die1 = Math.floor(Math.random() * 6) + 1;
  const die2 = Math.floor(Math.random() * 6) + 1;

  // END_BLOCK_DICE_GENERATION

  return [die1, die2];
}
// END_FUNCTION_rollDice
```

### 2. TypeScript строгая типизация

❌ **Избегай:**
- `any` типы
- Неявные типы
- Отсутствие return type

✅ **Используй:**
- Explicit типы для всех параметров
- Return types для всех функций
- Interfaces из `types/game.types.ts`

### 3. Immutable state updates

❌ **НЕ мутируй state напрямую:**
```typescript
gameState.players[0].resources.WOOD += 1; // ПЛОХО!
```

✅ **Создавай новый объект:**
```typescript
const updatedPlayer = {
  ...player,
  resources: {
    ...player.resources,
    WOOD: player.resources.WOOD + 1,
  },
};
```

### 4. Следуй roadmap

- ❌ Не прыгай между фазами
- ✅ Завершай текущую фазу полностью
- ✅ Проверяй Definition of Done из ImplementationRoadmap.md

### 5. Логирование

**Для клиентского кода используй console с префиксами:**
```typescript
console.log('[ModuleName][FunctionName][BLOCK_NAME][STATUS]', data);
```

Не используй `logger = logging.getLogger(__name__)` - это Python паттерн из template.

---

## 🔍 Быстрые команды для навигации

### Найти функцию по имени:
```bash
Grep pattern="START_FUNCTION_calculateLongestRoad" output_mode="content"
```

### Найти все валидаторы:
```bash
Grep pattern="canBuild" path="lib/game-logic/validators.ts" output_mode="content"
```

### Найти использования константы:
```bash
Grep pattern="BUILDING_COSTS" output_mode="files_with_matches"
```

### Найти все React компоненты:
```bash
Glob pattern="components/**/*.tsx"
```

### Понять структуру модуля:
```bash
Read knowledge_graph.xml
# Найди <lib_game_dash_logic_validators_ts_py>
# Посмотри на <CrossLinks> для зависимостей
```

---

## 📞 Частые вопросы

### Q: Как мне найти, где вызывается функция X?

**A:** Два способа:
1. **Через граф знаний:** Читай `knowledge_graph.xml`, ищи `<CrossLinks>` с `TYPE="CALLS_FUNCTION"` и `TARGET="function_name"`
2. **Через Grep:** `Grep pattern="functionName("` для всех вызовов

### Q: Какую фазу делать следующей?

**A:** Читай `docs/ImplementationRoadmap.md`, находи первую фазу со статусом `[ ]` (не ✅). Сейчас это **Phase 4 - Basic Gameplay**.

### Q: Как понять, что функция делает?

**A:**
1. Найди функцию через Grep: `START_FUNCTION_[name]`
2. Прочитай `FUNCTION_CONTRACT` - там PURPOSE, INPUTS, OUTPUTS
3. Изучи логические блоки `START_BLOCK_` с описаниями
4. Посмотри `LINKS` к связанным функциям

### Q: Можно ли использовать Tailwind v4?

**A:** ❌ НЕТ! Проект использует Tailwind CSS v3.4.1. Мы откатились с v4 из-за проблем с PostCSS. Это зафиксировано в `tailwind.config.js` (не .ts).

### Q: Почему в package.json нет "type": "commonjs"?

**A:** Next.js использует ES modules. Раньше было "type": "commonjs", но это вызывало ошибки при компиляции. Убрали это поле.

### Q: Где искать примеры кода с семантической разметкой?

**A:**
1. `docs/CodeGenerationGuide.md` - примеры всех типов файлов
2. Любой файл в `lib/` или `components/` - все следуют template
3. Лучший пример: `lib/game-logic/calculators.ts` - полная разметка

### Q: Как обновить граф знаний после добавления нового модуля?

**A:**
1. Открой `knowledge_graph.xml`
2. Найди секцию `<FutureDevelopment>` если это planned модуль
3. Замени `STATUS="PLANNED_PHASE_X"` на `STATUS="COMPLETED"`
4. Разверни placeholder в полное описание с функциями и CrossLinks
5. Обнови `<ProjectStatus COMPLETION_PERCENT="XX"/>`

---

## 🎯 Твоя первая задача (рекомендация)

Если ты новый AI агент и тебе нужно продолжить проект:

1. **Прочитай (5-10 минут):**
   - `docs/CLAUDE.md`
   - `docs/ImplementationRoadmap.md` (Phase 4 секция)
   - `docs/CodeGenerationGuide.md` (примеры Zustand store)

2. **Изучи уже созданный код (15 минут):**
   - `types/game.types.ts` - типы
   - `lib/game-logic/actionHandlers.ts` - как обрабатываются действия
   - `components/board/Board.tsx` - как рендерится карта

3. **Начни Phase 4 (работа):**
   - Создай `store/gameStore.ts`
   - Создай `app/game/page.tsx`
   - Реализуй начальную расстановку

**Время на onboarding:** ~20-30 минут
**Время до первого кода:** ~30 минут

---

## 📚 Дополнительные ресурсы

**В репозитории:**
- `knowledge_graph.xml` - полный граф проекта
- `docs/TechSpecification.md` - детальное ТЗ (~2700 строк)
- `docs/DevelopmentPlan.md` - XML граф + алгоритмы (~2700 строк)
- `docs/TechStackRationale.md` - почему выбрали эти технологии

**Внешние ссылки:**
- [Next.js 14 Documentation](https://nextjs.org/docs)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Zustand Documentation](https://github.com/pmndrs/zustand)
- [Catan Board Game Rules](https://www.catan.com/understand-catan/game-rules)

---

## ✅ Checklist для нового агента

Прежде чем начать кодить, убедись что:

- [ ] Прочитал `docs/CLAUDE.md` (правила работы с разметкой)
- [ ] Прочитал `docs/ImplementationRoadmap.md` Phase 4
- [ ] Изучил примеры в `docs/CodeGenerationGuide.md`
- [ ] Понимаешь структуру проекта (types, lib, components, app)
- [ ] Знаешь где искать информацию (граф знаний, Grep, Read)
- [ ] Понимаешь обязательность семантической разметки
- [ ] Знаешь что делать дальше (Phase 4 - Zustand store)

**Если всё ✅ - ты готов к работе!** 🚀

---

**Версия документа:** 1.0
**Для вопросов:** Используй граф знаний и документацию, там всё есть
**Успешной разработки!** 🎲🏘️
